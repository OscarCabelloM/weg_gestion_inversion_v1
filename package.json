{
  "name": "weg-gestion-inversion",
  "private": true,
  "version": "1.8",
  "description": "Gestión_Inversiones.v.1.8 — Gestor de portafolio de inversiones con datos en vivo de Yahoo Finance",
  "workspaces": [
    "frontend",
    "api"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "npm run dev --workspace frontend",
    "build": "npm run build --workspace frontend",
    "preview": "npm run preview --workspace frontend",
    "api": "node api/yahoo.js",
    "start": "node api/yahoo.js",
    "doctor": "npx react-doctor@latest"
  },
  "devDependencies": {
    "react-doctor": "^0.9.12"
  }
}
